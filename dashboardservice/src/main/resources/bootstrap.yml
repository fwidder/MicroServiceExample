spring:
    application:
        name: DashBoardService
    cloud:
        config:
            fail-fast: true
            uri: http://configserver:8081
            retry:
              max-attempts: 60
