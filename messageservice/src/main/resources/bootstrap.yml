spring:
    application:
        name: MessageService
    cloud:
        config:
            fail-fast: true
            uri: http://configserver:8081
            retry:
              max-attempts: 60
