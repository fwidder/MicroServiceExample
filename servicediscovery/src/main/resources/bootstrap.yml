server:
    port: 8082
spring:
    application:
        name: ServiceDiscovery
    cloud:
        config:
            fail-fast: true
            uri: http://configserver:8081
            retry:
              max-attempts: 60
