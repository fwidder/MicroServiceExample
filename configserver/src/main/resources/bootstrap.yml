server:
    port: 8081
spring:
    cloud:
        config:
            server:
                native:
                    searchLocations: ${user.home}/Developer/Privat/MicroServiceExample/configfiles
    profiles:
        active: native
