server:
    port: 8085
spring:
    application:
        name: BootAdmin
    cloud:
        config:
            fail-fast: true
            uri: http://configserver:8081
            retry:
              max-attempts: 60
